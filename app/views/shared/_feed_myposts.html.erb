
  <ol class="microposts">
    <%#= foreach post that meet search criteria%>

    <%= render partial: 'shared/feed_item', collection: current_user.myposts %>
  </ol>
  <%#= will_paginate @fav_items %>
